# C++点菜系统 - 知识点总结

## 一、数据结构选择

### 1.1 三种存储方式对比

在存储菜品、用户、台桌数据时，有三种选择：

| 方式 | 特点 |
|------|------|
| C 风格数组 `DISH dishes[100]` | 固定大小，简单但不灵活 |
| `std::vector<DISH>` | 动态大小，支持增删 |
| `std::map<int, DISH>` | 键值对，按 ID 快速查找 |

### 1.2 为什么选 vector？

**场景分析**：分页显示需要"按顺序取第 i 个元素"

```cpp
// 分页核心逻辑：取第 startIndex 到 endIndex 的数据
for(int i = startIndex; i < endIndex; i++) {
    DISH* dish = manager->getDishByIndex(i);  // 需要按索引取
}
```

**三种方式的效率对比**：

| 方式 | 按索引取 `arr[i]` | 按 ID 查找 |
|------|------------------|-----------|
| C 数组 | O(1) | O(n) 要遍历 |
| vector | O(1) | O(n) 要遍历 |
| map | O(n) 要遍历 | O(log n) |

**结论**：分页场景用 vector 最合适

### 1.3 vector 为什么索引是 O(1)？

**关键**：vector 底层是**连续内存**

```
内存布局：
地址:  1000    1004    1008    1012    1016
       [数据0] [数据1] [数据2] [数据3] [数据4]
       ↑
       起始地址
```

访问 `arr[3]` 时：

```
目标地址 = 起始地址 + 3 × 每个元素大小
        = 1000 + 3 × 4
        = 1012
```

**一次计算直接定位，不需要遍历！**

对比链表：

```
链表必须从头走：head → node1 → node2 → node3
要访问第3个，必须走3步，所以是 O(n)
```

### 1.4 什么时候用 map？

如果操作主要是"根据 ID 查找"：

```cpp
DISH* dish = manager->getDishByID(1003);  // 按 ID 查
```

那 map 更快（O(log n) vs vector 的 O(n)）

**本项目选择**：分页显示多，按 ID 查找少 → 选 vector

---

## 二、状态机模式（重点）

### 2.1 问题：递归调用的灾难

**原来的代码**：

```cpp
// CLoginWin::handle()
void CLoginWin::handle() {
    if(登录成功) {
        CAdminWin* win = new CAdminWin();
        win->show();
        win->run();
        win->handle();  // 在 handle 里又会创建新窗口...
    }
}

// CAdminWin::handle()
void CAdminWin::handle() {
    if(选择台桌管理) {
        CAdminWin_Table* win = new CAdminWin_Table();
        win->handle();  // 又嵌套...
    }
}
```

**调用栈**：

```
main()
  └── CInitWin::handle()
        └── CLoginWin::handle()
              └── CAdminWin::handle()
                    └── CAdminWin_Table::handle()
                          └── COrderWin::handle()
                                └── CAdminWin_Table::handle()
                                      └── ...无限嵌套
```

**问题**：
- 栈空间有限，嵌套太深会**栈溢出崩溃**
- 每个窗口对象都没被 delete，**内存泄漏**

### 2.2 解决方案：状态机

**核心思想**：用一个**主循环**控制所有窗口，窗口只负责"告诉主循环下一步去哪"

#### 2.2.1 定义所有状态

```cpp
enum AppState {
    STATE_EXIT = 0,      // 退出程序
    STATE_INIT,          // 1 - 初始界面
    STATE_REGISTER,      // 2 - 注册界面
    STATE_LOGIN,         // 3 - 登录界面
    STATE_ADMIN,         // 4 - 管理员主界面
    STATE_MANAGER,       // 5 - 经理界面
    STATE_WAITER,        // 6 - 服务员界面
    STATE_ADMIN_TABLE,   // 7 - 台桌管理
    STATE_ADMIN_DM,      // 8 - 菜品管理
    STATE_ADMIN_PM,      // 9 - 人员管理
    STATE_ORDER          // 10 - 点菜界面
};
```

#### 2.2.2 主循环结构

```cpp
int main() {
    int state = STATE_INIT;  // 初始状态
    
    while(state != STATE_EXIT) {
        system("cls");
        
        switch(state) {
            case STATE_INIT: {
                CInitWin* win = new CInitWin();
                win->show();
                win->run();
                win->handle();
                int result = win->result;
                delete win;  // 用完就删，不泄漏
                
                // 根据 result 决定下一个状态
                if(result == 1) state = STATE_LOGIN;
                else if(result == 2) state = STATE_REGISTER;
                else state = STATE_EXIT;
                break;
            }
            
            case STATE_LOGIN: {
                CLoginWin* win = new CLoginWin();
                // ... 同样的流程 ...
                delete win;
                
                if(result == 1) state = STATE_ADMIN;
                else if(result == 2) state = STATE_MANAGER;
                else state = STATE_INIT;
                break;
            }
            
            // ... 其他状态 ...
        }
    }
    return 0;
}
```

#### 2.2.3 窗口只设置 result

**修改前（错误）**：

```cpp
void CAdminWin_Table::handle() {
    if(flag == -1) {  // 按 ESC
        CAdminWin* win = new CAdminWin();  // 创建新窗口
        win->show();
        win->run();
        win->handle();  // 递归！
    }
}
```

**修改后（正确）**：

```cpp
void CAdminWin_Table::handle() {
    if(flag == -1) {  // 按 ESC
        this->result = 0;  // 只设置结果
        running = false;   // 退出当前窗口
        // 让 main 去决定下一步
    }
}
```

#### 2.2.4 状态转换图

```
                    ┌─────────────────────────────────────┐
                    ↓                                     │
    ┌──────────┐  登录  ┌──────────┐  台桌管理  ┌─────────────────┐
    │ 初始界面 │ ────→ │ 管理员界面 │ ────────→ │ 台桌管理界面    │
    │ STATE=1  │       │ STATE=4   │           │ STATE=7         │
    └──────────┘       └──────────┘           └─────────────────┘
         │                  │                        │
         │ 注册             │ 菜品管理               │ 开桌点菜
         ↓                  ↓                        ↓
    ┌──────────┐      ┌──────────┐            ┌──────────┐
    │ 注册界面 │      │ 菜品管理 │            │ 点菜界面 │
    │ STATE=2  │      │ STATE=8  │            │ STATE=10 │
    └──────────┘      └──────────┘            └──────────┘
```

#### 2.2.5 状态机的优点

| 优点 | 说明 |
|------|------|
| **无递归** | 所有窗口在同一层循环，栈深度恒定 |
| **无内存泄漏** | 每个窗口 new 完就 delete |
| **逻辑清晰** | 所有跳转逻辑集中在 main |
| **易于维护** | 新增窗口只需加一个 case |
| **易于调试** | 打印 state 就知道当前在哪 |

#### 2.2.6 result 的含义约定

**重要**：result 的值是**每个窗口自己定义的**，不是全局统一的！

```cpp
// CInitWin 的 result 含义：
// 0 = 退出程序
// 1 = 去登录
// 2 = 去注册

// CAdminWin 的 result 含义：
// 0 = 返回初始界面
// 1 = 去台桌管理
// 2 = 去菜品管理
// 3 = 去人员管理

// CAdminWin_Table 的 result 含义：
// 0 = 返回管理员界面
// 1 = 去点菜界面（并且 selectedTable 有值）
```

main 里根据不同窗口的 result 做不同处理。

---

## 三、遗忘点复习

### 3.1 enum 枚举

#### 基本语法

```cpp
enum AppState {
    STATE_EXIT = 0,   // 显式指定 = 0
    STATE_INIT,       // 自动 = 1
    STATE_REGISTER,   // 自动 = 2
    STATE_LOGIN       // 自动 = 3
};

// 使用
int state = STATE_INIT;  // state = 1
```

#### 自动编号规则

```cpp
enum Example {
    A,          // = 0（默认从0开始）
    B,          // = 1
    C = 10,     // = 10（显式指定）
    D,          // = 11（从上一个+1）
    E           // = 12
};
```

#### enum vs #define

| 特性 | enum | #define |
|------|------|---------|
| 本质 | 类型定义 | 文本替换 |
| 自动编号 | 支持 | 要手写每个值 |
| 调试 | 有些调试器显示名字 | 只显示数字 |
| 作用域 | 可放在类里限制 | 放 .h 会全局替换 |

**实际选择**：两个都能用，enum 更"现代"一点，但 #define 也没问题

### 3.2 strcmp 字符串比较

#### 错误写法

```cpp
char a[10] = "hello";
char b[10] = "hello";

if(a == b) {  // 错！比较的是地址，永远不相等
    printf("相等");
}
```

#### 正确写法

```cpp
#include <string.h>

if(strcmp(a, b) == 0) {  // 返回0表示相等
    printf("相等");
}
```

#### strcmp 返回值

| 返回值 | 含义 |
|--------|------|
| 0 | 两个字符串相等 |
| < 0 | 第一个字符串 < 第二个 |
| > 0 | 第一个字符串 > 第二个 |

### 3.3 赋值 vs 比较

#### 经典错误

```cpp
if(flag = -1) {  // 错！这是赋值，永远为真（-1非零）
    // ...
}
```

#### 正确写法

```cpp
if(flag == -1) {  // 对！这是比较
    // ...
}
```

#### 防御技巧

```cpp
if(-1 == flag) {  // 把常量放左边，写错会编译报错
    // 如果写成 -1 = flag，编译器会报错
}
```

---

## 四、分页逻辑速查

```cpp
// 核心变量
int currentPage = 0;   // 当前页（从0开始）
int pageSize = 5;      // 每页显示数量
int totalCount;        // 总数据量
int allPage;           // 总页数

// 计算总页数（向上取整）
allPage = (totalCount + pageSize - 1) / pageSize;

// 计算当前页的数据范围
int startIndex = currentPage * pageSize;
int endIndex = min(startIndex + pageSize, totalCount);

// 遍历当前页数据
for(int i = startIndex; i < endIndex; i++) {
    // 显示第 i 条数据
}

// 上一页
if(currentPage > 0) {
    currentPage--;
}

// 下一页
if(currentPage < allPage - 1) {
    currentPage++;
}
```

---

## 五、此次优化解决的问题清单

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 控件不显示 | 子类重复定义父类成员变量 | 删除子类中重复的成员 |
| 递归栈溢出 | handle() 里创建新窗口并调用 handle() | 改用状态机模式 |
| 内存泄漏 | new 的窗口没有 delete | 状态机中及时 delete |
| 字符串比较失败 | 用 == 比较 char[] | 改用 strcmp() == 0 |
| 条件永远为真 | if(flag = -1) 赋值 | 改为 if(flag == -1) |
| sprintf 报错 | 拼写成 springf | 改正拼写 |
| gotoxy 报错 | 缺少命名空间 | 改为 CTool::gotoxy() |

